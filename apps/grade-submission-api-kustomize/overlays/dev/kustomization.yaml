apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: grade-kustomize
#namePrefix: dev-
nameSuffix: -kustomize 

patches:
  - path: replicas-patch.yaml
    target:
      kind: Deployment
      name: grade-submission-api
